interface:
  display_name: "Codex Feature Main"
  short_description: "Implement features and dispatch immutable PR reviews."
  default_prompt: "Use $codex-feature-main to carry accepted requirements through implementation, review handoff, and traceability."
policy:
  allow_implicit_invocation: true
