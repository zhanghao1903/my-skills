interface:
  display_name: "Codex PR Review & Merge"
  short_description: "Review exact PR heads and merge through strict gates."
  default_prompt: "Use $codex-pr-review-merge to validate and process this workflow ReviewRequest."
policy:
  allow_implicit_invocation: false
