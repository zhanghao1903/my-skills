interface:
  display_name: "Codex Workflow Init"
  short_description: "Initialize three-role Codex feature workflows."
  default_prompt: "Use $codex-workflow-init to initialize the Codex feature workflow for this repository."
policy:
  allow_implicit_invocation: false
